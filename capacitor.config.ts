import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ang_ionic_photo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
